name = "ilm-ai-gateway"
main = "worker.js"
compatibility_date = "2026-07-19"
workers_dev = true

[vars]
# Replace this with the exact public web origin before deployment.
ALLOWED_ORIGIN = "https://app.example.com"

# Set GATEWAY_SECRET and provider keys with `wrangler secret put`.
